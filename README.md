# Spring-Boot-Webhook-Processor
Spring Boot service to ingest webhooks securely: HMAC signature verification, idempotency (no duplicate events), PostgreSQL persistence, status tracking (received/processed/failed), and outbound API calls with retry/backoff. Includes Docker Compose and curl examples.
